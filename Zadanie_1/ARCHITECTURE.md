# Architektúra AI Agenta

## 🏗️ Systémový diagram

```
┌─────────────────────────────────────────────────────────────┐
│                        POUŽÍVATEĽ                            │
│                 "Koľko je 25 krát 4?"                        │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                      AI AGENT (agent.py)                     │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  1. Prijme otázku od používateľa                      │  │
│  │  2. Pripraví kontext a definície nástrojov            │  │
│  │  3. Zavolá LLM API                                    │  │
│  │  4. Spracuje odpoveď a tool calls                     │  │
│  │  5. Vykoná požadované nástroje                        │  │
│  │  6. Pošle výsledky späť LLM                           │  │
│  │  7. Vráti finálnu odpoveď používateľovi               │  │
│  └───────────────────────────────────────────────────────┘  │
└────────────┬────────────────────────┬───────────────────────┘
             │                        │
             ▼                        ▼
┌────────────────────────┐  ┌──────────────────────────────┐
│    GEMINI LLM API      │  │    NÁSTROJE (Tools)          │
│                        │  │                              │
│ • Analýza otázky       │  │ ┌──────────────────────────┐ │
│ • Rozhodovanie         │  │ │  calculate()             │ │
│ • Generovanie tool     │  │ │  • add                   │ │
│   calls                │  │ │  • subtract              │ │
│ • Generovanie odpovede │  │ │  • multiply              │ │
│                        │  │ │  • divide                │ │
│ Model: gemini-1.5-flash│  │ └──────────────────────────┘ │
└────────────────────────┘  │                              │
                            │ ┌──────────────────────────┐ │
                            │ │  get_current_time()      │ │
                            │ │  (advanced verzia)       │ │
                            │ └──────────────────────────┘ │
                            │                              │
                            │ ┌──────────────────────────┐ │
                            │ │  roll_dice()             │ │
                            │ │  (advanced verzia)       │ │
                            │ └──────────────────────────┘ │
                            │                              │
                            │ ┌──────────────────────────┐ │
                            │ │  get_weather()           │ │
                            │ │  (advanced verzia)       │ │
                            │ └──────────────────────────┘ │
                            └──────────────────────────────┘
```

## 🔄 Sekvenčný diagram (flow)

```
Používateľ          Agent           LLM API         Nástroj
    │                 │                 │              │
    │─────────────────>│                 │              │
    │  "25 krát 4?"   │                 │              │
    │                 │                 │              │
    │                 │─────────────────>│              │
    │                 │  prompt + tools │              │
    │                 │                 │              │
    │                 │<─────────────────│              │
    │                 │  function_call  │              │
    │                 │                 │              │
    │                 │─────────────────────────────────>│
    │                 │     calculate(multiply, 25, 4)  │
    │                 │                 │              │
    │                 │<─────────────────────────────────│
    │                 │          result: 100           │
    │                 │                 │              │
    │                 │─────────────────>│              │
    │                 │  function_result│              │
    │                 │                 │              │
    │                 │<─────────────────│              │
    │                 │   final_answer  │              │
    │                 │                 │              │
    │<─────────────────│                 │              │
    │  "Výsledok je   │                 │              │
    │     100"        │                 │              │
    │                 │                 │              │
```

## 📦 Dátové štruktúry

### Tool Definition (JSON)
```json
{
  "type": "function",
  "function": {
    "name": "calculate",
    "description": "Vykonáva základné matematické operácie",
    "parameters": {
      "type": "object",
      "properties": {
        "operation": {
          "type": "string",
          "enum": ["add", "subtract", "multiply", "divide"]
        },
        "a": {"type": "number"},
        "b": {"type": "number"}
      },
      "required": ["operation", "a", "b"]
    }
  }
}
```

### Function Call (od LLM)
```json
{
  "name": "calculate",
  "arguments": {
    "operation": "multiply",
    "a": 25,
    "b": 4
  }
}
```

### Function Response (do LLM)
```json
{
  "name": "calculate",
  "response": {
    "result": 100
  }
}
```

## 🧩 Komponenty

### 1. **Agent Core** (`agent.py`)
- Hlavná logika agenta
- Orchestrácia komunikácie medzi používateľom, LLM a nástrojmi
- Spracovanie tool calls

### 2. **Tool Registry**
- Definície dostupných nástrojov
- Mapovanie názvov na funkcie
- Validácia parametrov

### 3. **LLM Interface**
- Komunikácia s Gemini API
- Formátovanie promptov
- Parsovanie odpovedí

### 4. **Tools/Functions**
- Python funkcie vykonávajúce skutočnú prácu
- `calculate()` - matematické výpočty
- `get_current_time()` - aktuálny čas
- `roll_dice()` - náhodné čísla
- `get_weather()` - počasie

## 🔐 Bezpečnosť

```
Environment Variables (.env)
    ↓
load_dotenv()
    ↓
GEMINI_API_KEY
    ↓
genai.configure(api_key=...)
```

**.gitignore** zabezpečuje, že citlivé údaje sa nedostanú do Git repozitára.

## 📊 Možnosti rozšírenia

1. **Viac nástrojov**
   - Databázové operácie
   - API volania
   - Súborové operácie

2. **Pokročilé funkcie**
   - Chaining viacerých tool calls
   - Error handling a retry logika
   - Logovanie a monitoring

3. **Multi-agent systémy**
   - Špecializovaní agenti
   - Kooperácia medzi agentmi
   - Delegovanie úloh

4. **Personalizácia**
   - Ukladanie histórie konverzácií
   - Učenie sa z predchádzajúcich interakcií
   - Používateľské preferencie
